import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
    buttonRow: {
        textAlign: 'center',
        marginTop: 24
    },
    container: {
        padding: 0,
        maxWidth: 600
    }
}))