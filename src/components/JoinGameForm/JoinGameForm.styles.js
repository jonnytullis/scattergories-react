import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
    buttonRow: {
        marginTop: 24
    },
    center: {
        textAlign: 'center'
    },
    input: {
        fontSize: 50,
        textAlign: 'center',
        maxWidth: 250
    }
}))